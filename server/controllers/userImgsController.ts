import * as express from 'express';

import db from './connectToDb';

const userImgController = {

  addImageToUser: (req: express.Request, res: express.Response, next: express.NextFunction) => {

  },

  deleteImageForUser: (req: express.Request, res: express.Response, next: express.NextFunction) => {

  }
}

export default userImgController;
